# Requirements for LangExtract Gradio Web Interface
# 
# Install with: pip install -r requirements.txt
#
# For development setup, use: pip install -e .[gradio,dev]

# Core LangExtract package (install from current directory)
-e .

# Gradio web interface
gradio>=4.0.0

# Document processing support
pypdf>=5.0.0
python-docx>=1.0.0

# HTTP requests (already included in langextract dependencies, but explicit for clarity)
requests>=2.25.0
